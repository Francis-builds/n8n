build:
  docker:
    web: docker/images/n8n/Dockerfile

# Optionally define environment variables here (or set them in Heroku "Config Vars").
# Typically you'll need N8N_PORT to match Heroku's $PORT, plus any other required vars.
#config:
 # N8N_PORT: 8080
  #N8N_HOST: 0.0.0.0
  # ...any other ENV variables, e.g. N8N_BASIC_AUTH_USER, N8N_BASIC_AUTH_PASSWORD, etc.

run:
  web: n8n
